{
  "collections": {
    "users": {
      "rules": "allow read, write: if request.auth != null && request.auth.uid == userId;",
      "subcollections": {
        "entries": {
          "rules": "allow read, write: if request.auth != null && request.auth.uid == userId;"
        },
        "settings": {
          "rules": "allow read, write: if request.auth != null && request.auth.uid == userId;"
        }
      }
    }
  }
}
